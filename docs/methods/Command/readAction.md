# readAction([string $Name])
This method is used to read the contents of a log file. It takes one parameter, the name of the log file to read.

```command-line interface
./cli logger read example
```
